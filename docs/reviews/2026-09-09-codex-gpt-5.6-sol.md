## P1

- [P1, 10/10] [report.py:34](src/teyla/report.py:34), [monitor.py:49](src/teyla/monitor.py:49) — default Markdown exposes project identifiers. Codex/Grok/Hermes use absolute CWDs; Claude uses a path-derived slug. Input `/Users/acme/repos/secret-merger` appears in the “By project” table. This contradicts “aggregates only” and “no paths.”

- [P1, 10/10] [advise.py:60](src/teyla/advise.py:60), [advise.py:64](src/teyla/advise.py:64), [report.py:54](src/teyla/report.py:54) — JSON removes `correction_samples` but A9 copies a sample into `findings[].evidence`, restoring transcript content. Input `Never paste token sk-live-123 again` repeated twice leaks the secret in both Markdown and JSON.

- [P1, 10/10] [adapters/__init__.py:18](src/teyla/adapters/__init__.py:18) — the correction regex misses ordinary `No, use uv` and `No. Use uv`: the final `\b` follows punctuation, so punctuation-space/end has no word boundary. This materially understates correction rate and disables A5/A9. Direct probe confirmed both inputs return false.

## P2

- [P2, 9/10] [codex.py:157](src/teyla/adapters/codex.py:157) — adds `reasoning_output_tokens` to `output_tokens`. OpenAI usage treats reasoning as output-token detail, so `{output_tokens:80, reasoning_output_tokens:20}` becomes 100, inflating output, cost, cache ratio denominator, and A2.

- [P2, 10/10] [advise.py:44](src/teyla/advise.py:44) — A6 counts only `Agent`/`Task`, while Codex records `spawn_agent` and Hermes records `async_delegation`. Failing metric: 51 `spawn_agent` calls and zero review skills produces no A6. Use `sum(subagents.values())`.

- [P2, 9/10] [claude_code.py:49](src/teyla/adapters/claude_code.py:49) — any system JSON line containing “compact” increments compactions. A system message such as `Use compact output` can make A3 fire falsely; inspect the actual subtype instead.

- [P2, 9/10] [prompts/adoption-review.md:76](prompts/adoption-review.md:76), [prompts/adoption-review.md:100](prompts/adoption-review.md:100) — “correction shapes” are the first 50 normalized characters of real prompts, not anonymous aggregates. Names, ticket IDs, paths, or secrets survive.

## A1–A10

A3’s 8 MB/12 h/3-compaction thresholds and A1’s ≥10-call sample floor are reasonable operational alerts, but not empirically defended. A1 is unsoundly titled: inheritance means “parent model,” not necessarily “orchestrator.” A2’s 60%/1M floor is a budget preference, not a quality rule. A4’s 150× has no cost or latency normalization. A5 always emits after 50 turns, even at 0%, creating non-findings. A6 is broken cross-harness. A7/A8/A10 are defensible configuration checks. A9 should use fingerprints, never content, and its action names nonexistent `teyla rule add`.

## Positioning

The weakest claim is README:87: “None track … subagent model mix.” It is an absolute, quickly stale competitor claim, while current ccusage already spans Claude, Codex, Hermes, and Grok and exposes session/model usage. Narrow it to a dated feature matrix. Teyla also understates overlap with gstack’s persistent learnings and TRACE’s actual compiled enforcement; Teyla currently records/proposes rules rather than enforcing completion checks. [ccusage documentation](https://ccusage.com/guide/), [gstack skills](https://github.com/garrytan/gstack/blob/main/docs/skills.md), [TRACE paper](https://arxiv.org/abs/2606.13174).

## Cut / add

Cut: “operating system”; verbatim correction storage in reports; scaffold/products from the core adoption-monitor pitch.

Add: irreversible redaction/pseudonymous project IDs; schema-versioned adapter fixtures from real logs; per-rule denominator, confidence, and configurable thresholds.

**DONE_WITH_CONCERNS:** read-only review completed; no files changed.